apiVersion: v1
kind: Secret
metadata:
  name: {{ include "immich.fullname" . }}-server-env
  labels:
    {{- include "immich.labels" . | nindent 4 }}
type: Opaque
data:
  {{- include "immich.server.secretEnv" . | nindent 2 }}
